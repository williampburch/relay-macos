# FreeRDP bridge boundary

The future C/Swift bridge for FreeRDP belongs in this directory. Milestone 1 keeps this
subsystem empty so the connection library, credentials, and session tabs remain independent
of the eventual rendering and input implementation.
